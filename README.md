# ClearBlocks
A tool to improve AI transparency by leveraging blockchain technology. The metadata of the AI model is tracked, hashed, and stored on the blockchain. The metadata that was stored can then later be used to audit the AI model.

## PIP package
https://test.pypi.org/project/clearblocks-anjana/
